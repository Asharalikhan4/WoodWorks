import React from "react";

export default function ContactUsPage(): JSX.Element {
    return (
        <div className="md:container md:mx-auto">
            <div className="text-[#121212] text-center md:text-5xl">Contact Us</div>
            <div className="md:text-xl">Need information about a product (availability, characteristics, etc.) ? Please fill in the form below.</div>
            <p>Email:</p>
        </div>
    );
};
